function toArray (x,y) {
    const array = [...[x,y]]
    return array
    };
    
    console.log(toArray(3,10));

    //spread operuje na elementach iterowalnych, w przeciwieństwie do poprzednio statycznych argumentów funkcji